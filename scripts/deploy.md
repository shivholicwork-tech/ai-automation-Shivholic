# Deployment

## Vercel (web)
- Root directory: `apps/web`
- Build command: `npm run build -w @shivholic/web`

## Railway/Render (api)
- Root directory: repo root
- Start command: `npm run start -w @shivholic/api`

## Supabase
- Provision PostgreSQL and set `DATABASE_URL`.
